import React from "react";
import {Text, View} from "react-native";
import {styles} from "../Styles";

export const ReadingScreen = () => {
    return(
        <View style={styles.container}>
            <Text>This is the reading selection screen</Text>
        </View>
    );
}